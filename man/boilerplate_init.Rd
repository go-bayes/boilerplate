% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/unified-init-functions.R
\name{boilerplate_init}
\alias{boilerplate_init}
\title{Initialize All Boilerplate Databases}
\usage{
boilerplate_init(
  categories = c("measures", "methods", "results", "discussion", "appendix", "template"),
  merge_strategy = c("keep_existing", "merge_recursive", "overwrite_all"),
  data_path = NULL,
  quiet = FALSE,
  dry_run = FALSE,
  create_dirs = FALSE,
  confirm = TRUE
)
}
\arguments{
\item{categories}{Character vector. Categories to initialize.
Default is all categories: "measures", "methods", "results", "discussion", "appendix", "template".}

\item{merge_strategy}{Character. How to merge with existing databases: "keep_existing", "merge_recursive", or "overwrite_all".}

\item{data_path}{Character. Base path for data directory.
If NULL (default), uses here::here("boilerplate", "data").}

\item{quiet}{Logical. If TRUE, suppresses all CLI alerts. Default is FALSE.}

\item{dry_run}{Logical. If TRUE, simulates the operation without writing files. Default is FALSE.}

\item{create_dirs}{Logical. If TRUE, creates directories that don't exist. Default is FALSE.}

\item{confirm}{Logical. If TRUE, asks for confirmation before making changes. Default is TRUE.}
}
\value{
Invisibly returns a logical vector indicating which categories were successfully initialized.
}
\description{
This function initializes or updates all boilerplate databases with default values.
It's a convenience wrapper around boilerplate_init_category() for all categories.
}
\examples{
\dontrun{
# run in dry-run mode first to see what would happen
boilerplate_init(dry_run = TRUE)

# initialize all databases with confirmation prompts
boilerplate_init(create_dirs = TRUE)

# initialize specific categories only
boilerplate_init(
  categories = c("methods", "measures"),
  create_dirs = TRUE,
  confirm = FALSE
)
}

}
